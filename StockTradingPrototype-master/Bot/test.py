from Bot.sms_bot import SmsBot
from Bot.sms_handler import SmsHandler
from threading import Thread
import time


if __name__ == "__main__":
    # bot = SmsBot()
    # bot.start()
    # sms_handler = SmsHandler(twilio_sid=bot.TWILIO_ACCOUNT_SID_TRIAL, twilio_auth_token=bot.TWILIO_AUTH_TOKEN_TRIAL,
    #                          owned_phone_number=bot.TWILIO_NUMBER_TRIAL, allowed_sender_phone=bot.TWILIO_NUMBER_PAID)
    # sms_handler.wait_for_inbound_sms(on_received_sms_listener=bot.sms_received, refresh_after=2)
    # time.sleep(10)
    # sms_handler.send_message(to=bot.TWILIO_NUMBER_PAID, body="SYMBOL 18", on_send_message_listener=bot.sms_received)
    pass

